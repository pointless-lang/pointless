fn factors(num)
  assert(Math.isInt(num))
  assert(num > 1)

  div = 2
  result = []

  while div <= Math.sqrt(num) do
    if num % div == 0 then
      result |= push(div)
      num /= div
    else
      div += 1
    end
  end

  push(result, num)
end

print(factors(60))
