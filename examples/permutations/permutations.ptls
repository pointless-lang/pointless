fn permutations(elems)
  if elems == [] then 
    [[]]
  else
    elems
      $ subPermute(elems)
      | List.merge
  end
end

fn subPermute(elem, elems)
  elems
    | remove(elem)
    | permutations
    $ [elem] + arg
end

"tea"
  | chars
  | permutations
  $ join("")
  | print
