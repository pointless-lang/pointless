fn getChars(word)
  word
    | chars
    | sort
    | join("")
end

groups = import "lines:unixdict.txt"
  | List.groupBy(getChars)
  | Obj.values

maxLen = groups
  $ len
  | max

groups
  ? len(arg) == maxLen
  | print
