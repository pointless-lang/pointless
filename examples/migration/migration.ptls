cities = import "csv:cities.csv"

cities
  | select("state")
  | Table.counts
  | take(10)
  | print

cities.change = cities $ roundTo(arg.pop2024 / arg.pop2020 * 100 - 100, 2)

cities.density = cities $ roundTo(arg.pop2024 / arg.area, 2)

cities
  | Table.top("change", 10)
  | print

cities
  | Table.top("density", 10)
  | print
