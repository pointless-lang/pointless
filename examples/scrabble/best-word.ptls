points = import "json:points.json"
text = import "text:alice.txt"

fn wordInfo(word)
  score = word
    | chars
    $ points[arg]
    | sum

  { word, score }
end

text
  | split(" ")
  | List.unique
  $ wordInfo
  | Table.of
  | Table.maxBy("score")
  | print