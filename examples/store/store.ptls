customers = import "csv:data/customers.csv"
invoices = import "csv:data/invoices.csv"

invoices
  | Table.join(customers, "customerId")
  | select(["firstName", "lastName", "total", "country"])
  | summarize("country", Table.average)
  | sortDescBy("total")
  | roundTo(2)
  | print
